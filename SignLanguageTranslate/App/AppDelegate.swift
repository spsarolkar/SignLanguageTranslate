import UIKit

class AppDelegate: NSObject, UIApplicationDelegate {
    // MARK: - Background URL Session Handling
    // Placeholder for future background download session configuration
    // This will be used to handle background URL session completion events
    // for dataset downloads when the app is not in the foreground

    func application(
        _ application: UIApplication,
        didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey : Any]? = nil
    ) -> Bool {
        return true
    }
}
